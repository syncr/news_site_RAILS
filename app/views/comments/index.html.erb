<div class="row">
  <div class='small-12'>
<h1>Comments for <%= @link.headline %></h1>

<%= render 'form'%>

<% @comments.each do |comment| %>
  <p class = 'comment'><%= comment.content %></p>
<% end %>
